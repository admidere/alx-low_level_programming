#include "main.h"
/**
* _abs -  absolute value of an integer.
* @n: is the int that will use for the argument of the function
* return: 0
*/
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	return (n*-1);
}
