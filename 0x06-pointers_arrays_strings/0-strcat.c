#include "main.h"
#include <string.h>

/**
* _strcat - called function
* @*dest: destination parameter
* @*src: source function
* return: destination
*/

char *_strcat(char *dest, char *src)
{
strcat(dest, src);
return (dest);
}
