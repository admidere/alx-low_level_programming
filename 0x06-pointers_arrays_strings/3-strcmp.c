#include "main.h"
#include <string.h>

/**
* _strcmp - function
* @s1: parameter
* @s2: parameter
* cmp
*/

int _strcmp(char *s1, char *s2)
{
	strcmp(s1, s2);
	strcmp(s2, s1);
	strcmp(s1, s1);

	return (0);	
}
