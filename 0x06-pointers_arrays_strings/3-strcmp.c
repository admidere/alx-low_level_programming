#include "main.h"
#include <string.h>

/**
*  _strcmp - Function which compare two strings and
*@s1: first string
*@s2:second string
*Return: cmp
*/

int _strcmp(char *s1, char *s2)
{
int cmp;

cmp = strcmp(s1, s2);

return (cmp);
}
